\begin{tikzpicture}[node distance=2cm, auto]
    % Nodes
    \node [startstop] (start) {Start};
    \node [process, below of=start] (input) {Input Data};
    \node [decision, below of=input] (decide) {Valid?};
    \node [process, right of=decide, node distance=3cm] (process) {Process Data};
    \node [data, below of=process] (output) {Output Result};
    \node [startstop, below of=decide] (error) {Show Error};
    \node [startstop, below of=output] (end) {End};
    
    % Arrows
    \draw [arrow] (start) -- (input);
    \draw [arrow] (input) -- (decide);
    \draw [arrow] (decide) -- node {yes} (process);
    \draw [arrow] (decide) -- node {no} (error);
    \draw [arrow] (process) -- (output);
    \draw [arrow] (output) -- (end);
    \draw [arrow] (error) -| node[near start] {retry} (input);
\end{tikzpicture}
